name: Tutor Service CI

on:
  push:
    branches: ["main"]
    paths:
      - 'backend/services/tutor-service/**'
      - '.github/workflows/tutor-service-ci.yml'

jobs:
  build-and-push-tutor-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Tutor Service
        uses: docker/build-push-action@v5
        with:
          context: ./backend/services/tutor-service
          file: ./backend/services/tutor-service/docker/Dockerfile.prod
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/lernen-tutor-service:latest

      - name: Trigger OnRender Deploy for Tutor Service
        run: curl -X POST ${{ secrets.ONRENDER_HOOK_TUTOR_SERVICE }}
