name: Admin Frontend CI

on:
  push:
    branches: ["main"]
    paths:
      - 'frontend/admin/**'
      - '.github/workflows/admin-ci.yml'

jobs:
  build-and-push-admin-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Admin Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend/admin
          file: ./frontend/admin/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/lernen-admin-aws:latest

      - name: Setup SSH Key
        run: |
          echo "${{ secrets.EC2_SSH_KEY }}" > private_key.pem
          chmod 600 private_key.pem

      - name: Deploy to AWS EC2
        run: |
          ssh -o StrictHostKeyChecking=no -i private_key.pem ubuntu@${{ secrets.AWS_LERNEN_ADMIN }} << 'EOF'
            # Login to Docker Hub
            docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_TOKEN }}
            
            # Pull latest image
            docker pull ${{ secrets.DOCKER_USERNAME }}/lernen-admin-aws:latest
            
            # Stop and remove existing container if exists
            docker stop lernen-admin || true
            docker rm lernen-admin || true
            
            # Run new container
            docker run -d \
              --name lernen-admin \
              --restart unless-stopped \
              -p 80:80 \
              -p 443:443 \
              ${{ secrets.DOCKER_USERNAME }}/lernen-admin-aws:latest
            
            # Cleanup old images
            docker image prune -f
          EOF

      - name: Cleanup SSH Key
        if: always()
        run: rm -f private_key.pem
