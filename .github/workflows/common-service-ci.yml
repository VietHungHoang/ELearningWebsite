name: API Gateway CI

on:
  push:
    branches: ["main"]
    paths:
      - 'backend/services/common-service/**'
      - '.github/workflows/common-service-ci.yml'

jobs:
  build-and-push-api-gateway:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Common Service
        uses: docker/build-push-action@v5
        with:
          context: ./backend/services/common-service
          file: ./backend/services/common-service/docker/Dockerfile.prod
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/lernen-common-service:latest
          
      - name: Trigger OnRender Deploy for Common Service
        run: curl -X POST ${{ secrets.ONRENDER_HOOK_COMMON_SERVICE }}