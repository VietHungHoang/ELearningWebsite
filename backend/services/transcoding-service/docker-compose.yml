# services:
#   transcoding-service:
#     build:
#       context: ..
#       dockerfile: docker/Dockerfile.dev
#     container_name: transcoding-service
#     ports:
#       - "8085:8085"
#     environment:
#       - SPRING_PROFILES_ACTIVE=docker
#       - AWS_REGION=us-east-1
#       - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
#       - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
#       # Override default values for local development
#       - AWS_S3_RAW_VIDEOS_BUCKET=raw-videos-dev
#       - AWS_S3_STREAMING_ASSETS_BUCKET=streaming-assets-dev
#       - AWS_SQS_TRANSCODING_QUEUE=video-transcoding-queue-dev
#       - AWS_SQS_DEAD_LETTER_QUEUE=video-transcoding-dlq-dev
#       - COURSE_SERVICE_BASE_URL=http://course-service:8081
#       - PROCESSING_WORKSPACE_ROOT=/tmp/transcoding
#       - PROCESSING_MAX_CONCURRENT_JOBS=2
#       - FFMPEG_BINARY_PATH=ffmpeg
#     volumes:
#       # Mount a volume for transcoding workspace (optional, for persistence during development)
#       - transcoding_workspace:/tmp/transcoding
#       # Mount logs directory (optional)
#       - ./logs:/app/logs
#     networks:
#       - elearning-network
#     depends_on:
#       - localstack
#     restart: unless-stopped
#     healthcheck:
#       test: ["CMD", "curl", "-f", "http://localhost:8085/api/transcoding/health"]
#       interval: 30s
#       timeout: 10s
#       retries: 3
#       start_period: 40s

#   # LocalStack for local AWS services simulation
#   localstack:
#     image: localstack/localstack:latest
#     container_name: localstack
#     ports:
#       - "4566:4566"
#       - "4571:4571"
#     environment:
#       - SERVICES=s3,sqs
#       - DEBUG=1
#       - DATA_DIR=/tmp/localstack/data
#       - DOCKER_HOST=unix:///var/run/docker.sock
#     volumes:
#       - "/tmp/localstack:/tmp/localstack"
#       - "/var/run/docker.sock:/var/run/docker.sock"
#     networks:
#       - elearning-network

# volumes:
#   transcoding_workspace:
#     driver: local

# networks:
#   elearning-network:
#     external: true