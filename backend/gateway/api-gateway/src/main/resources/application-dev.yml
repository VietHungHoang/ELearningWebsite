spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080
  cloud:
    gateway:
      routes:
        - id: bff-route
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/web/**
            - Path=/api/v1/public/**
          filters:
            - RewritePath=/api/v1/(web|public)/(.*), /api/v1/bff/$2

        - id: auth-service-route
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/auth/**