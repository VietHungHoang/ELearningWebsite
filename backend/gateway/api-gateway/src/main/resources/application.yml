spring:
  application:
    name: api-gateway
  profiles:
    active: dev

server:
  port: 9090

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG

